{
  "name": "jwtlab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install --include=dev && ./node_modules/.bin/vite build",
    "start": "node server/src/index.js",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "node server/src/database/seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
